spring.datasource.url=jdbc:mysql://localhost:3306/pta_qldvcc
spring.datasource.username=root
spring.datasource.password=pta19122004

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# JWT Configuration
jwt.secret=yourVeryLongSecretKeyHereMakeItAtLeast256Bits
jwt.expiration=86400

# Server Configuration
server.port=8080

# Spring Security Debug (optional, for debugging)
logging.level.org.springframework.security=DEBUG

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads

# Static Resource Configuration
spring.web.resources.static-locations=file:${file.upload-dir}/
spring.mvc.static-path-pattern=/uploads/**